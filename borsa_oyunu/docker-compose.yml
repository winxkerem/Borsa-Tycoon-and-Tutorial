version: '3.8'

services:
  web:
    build: .
    container_name: borsa_simulasyonu
    ports:
      - "5000:5000"
    volumes:
      - ./instance:/app/instance
    environment:
      - SECRET_KEY=borsa-simulasyonu-gizli-anahtar-1923
      - FLASK_APP=run.py
      - FLASK_ENV=production
    restart: always
